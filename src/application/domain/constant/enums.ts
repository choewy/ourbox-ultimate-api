export enum UserType {
  Admin = 'admin',
  User = 'user',
}

export enum UserStatus {
  Activated = 'activated',
  Disabled = 'disabled',
}

export enum ProductType {
  Single = 'single',
  Combo = 'combo',
}

export enum ProductUnit {
  Each = 'ea',
  Case = 'cs',
  Package = 'pkg',
}
